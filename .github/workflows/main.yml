name: First Workflow
 on:
   push:
     branches:
       - main:
jobs:
  example-job:
    runs-on: ubuntu-latest
    steps:
      - name: print a welcome message
        run: echo "Welcome to my first GitHub Actions Workflow!"

      - name: Checkout reository 
        uses: actions/checkout@v6.0.1 
      - name: List reopsitory files 
        run: ls -la
